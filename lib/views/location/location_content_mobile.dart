import 'package:flutter/material.dart';

class LocationContentMobile extends StatelessWidget {
  const LocationContentMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Location Content Mobile'),
      ),
    );
  }
}
