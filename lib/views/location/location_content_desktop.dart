import 'package:flutter/material.dart';

class LocationContentDesktop extends StatelessWidget {
  const LocationContentDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Location Content Desktop'),
      ),
    );
  }
}
