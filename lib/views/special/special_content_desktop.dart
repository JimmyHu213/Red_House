import 'package:flutter/material.dart';

class SpecialContentDesktop extends StatelessWidget {
  const SpecialContentDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Special Content Desktop'),
      ),
    );
  }
}
