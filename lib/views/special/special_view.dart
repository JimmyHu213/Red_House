import 'package:flutter/material.dart';

class SpecialView extends StatelessWidget {
  const SpecialView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Special View'));
  }
}
